type Loader {
    identifier: String!
    name: String!
}
type DataDescriptor {
    identifier: String!
    name: String!
    version: String!
    topic: String!
    topicPartitionCount: Int

}
type Query {
    # a type of query which returns all links
    loaders: [Loader!]!
}

schema {
    query: Query
}